services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile

    image: thesis-discord-bot:latest

    env_file:
      - .env

    volumes:
      - ./resources:/app/resources
      - ./data:/app/data
      - ./logs:/app/logs

    restart: unless-stopped